{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run check && npx vite build && npx esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist --target=node18"
  },
  "start": {
    "startCommand": "cd dist && node index.js"
  }
}